# General requirements
jsonargparse==4.18.0
loguru==0.6.0
snowflake-connector-python==3.0.1  # Adjust version as per your usage

# Dependencies related to the project
semantic_model_generator==1.0.0  # Add the correct version for your internal library
protobuf==4.23.1  # For protobuf handling

# Additional dependencies (if any)
pandas==2.0.3  # If get_valid_schemas_tables_columns_df uses pandas DataFrame
